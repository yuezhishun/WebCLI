# 版本演进规则

你们很可能会演进协议（patch 变 diff、styles 变全局字典、引入压缩等），建议从 v1 就立规矩：

1. **v 只在破坏性变更时升级**（字段新增不升级）
2. **新增字段必须可选**（服务端发了新字段，旧客户端忽略）
3. **客户端必须容忍未知 type**（记录日志并忽略）
4. **snapshot 是“最终一致性锚点”**：任何异常都能靠 resync 回归一致
